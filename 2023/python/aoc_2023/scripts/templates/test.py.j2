"""
Advent of Code 2023, Day {{ day }}: {{ title }}.
"""

import io
from typing import TextIO

import pytest

from aoc_2023.day{{ day }} import part_one, part_two

EXAMPLE = """\

"""


@pytest.fixture()
def example() -> str:
    return EXAMPLE


def test_part_one(example: str) -> None:
    """
    Test that checks if the solution for part one works on the example input.
    """
    assert part_one(io.StringIO(example)) == 42


def test_part_two(example: str) -> None:
    """
    Test that checks if the solution for part two works on the example input.
    """
    assert part_two(io.StringIO(example)) == 42
